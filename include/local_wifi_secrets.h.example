#pragma once

// Copy this file to include/local_wifi_secrets.h and enter the credentials for
// the firmware you build locally. The destination file is gitignored and is
// never included in GitHub source, CI artifacts, or public releases.
namespace local_wifi {

inline constexpr char kSsid[] = "YOUR_WIFI_NAME";
inline constexpr char kPassword[] = "YOUR_WIFI_PASSWORD";

}  // namespace local_wifi
